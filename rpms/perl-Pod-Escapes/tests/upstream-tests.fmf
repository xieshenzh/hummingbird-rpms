summary: Upstream tests
component: perl-Pod-Escapes
require: perl-Pod-Escapes-tests
test: /usr/libexec/perl-Pod-Escapes/test
enabled: true
tag:
  - rhel-buildroot
adjust:
  - enabled: false
    when: distro < rhel-10 or distro < centos-stream-10
    continue: false
