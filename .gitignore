builds/
__pycache__
.cache/
.dist_git_update_state.json

# Source tarballs (stored in lookaside cache, referenced via sources file)
# Generate checksums with: sha512sum --tag <file>
rpms/*/*.tar.gz
rpms/*/*.tar.bz2
rpms/*/*.tar.xz
rpms/*/*.tar.zst
