fetch:
  - type: git
    repo: "${UPSTREAM_REPO}"
    ref: "v${VERSION}"
    archive_name: "trivy-${VERSION}.tar.gz"
  - type: vendor
    ecosystem: go
    archive_name: "trivy-${VERSION}-vendor.tar.bz2"
