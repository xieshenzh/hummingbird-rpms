fetch:
  - type: git
    repo: "${UPSTREAM_REPO}"
    ref: "v${VERSION}"
    archive_name: "opentofu-${VERSION}.tar.gz"
  - type: vendor
    ecosystem: go
    archive_name: "opentofu-${VERSION}-vendor.tar.bz2"
