fetch:
  - type: git
    repo: "${UPSTREAM_REPO}"
    ref: "v${VERSION}"
    archive_name: "spire-${VERSION}.tar.gz"
  - type: vendor
    ecosystem: go
    archive_name: "spire-${VERSION}-vendor.tar.bz2"
