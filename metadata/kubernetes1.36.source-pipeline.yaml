fetch:
  - type: git
    repo: "${UPSTREAM_REPO}"
    ref: "v${VERSION}"
    archive_name: "kubernetes-${VERSION}.tar.gz"
  - type: vendor
    ecosystem: go
    archive_name: "kubernetes-${VERSION}-vendor.tar.bz2"
