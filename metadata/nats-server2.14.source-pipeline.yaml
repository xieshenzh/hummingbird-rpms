fetch:
  - type: git
    repo: "${UPSTREAM_REPO}"
    ref: "v${VERSION}"
    archive_name: "nats-server-${VERSION}.tar.gz"
  - type: vendor
    ecosystem: go
    archive_name: "nats-server-${VERSION}-vendor.tar.bz2"
